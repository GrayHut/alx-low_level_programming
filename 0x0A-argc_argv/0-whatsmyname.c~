#include <stdio.h>
#include "main.h"
/**
 *main-> program prints its name after compilation,
 *beginning of the program.
 *@argc: first parameter of main function holding the count for the argv[].
 *@argv: second argument for main holding the character strings in the
 *command line.
 *Return: 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	while (argv[0])
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
