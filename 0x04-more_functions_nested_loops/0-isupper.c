#include <stdio.h>

/**
 *_isupper() - determines if a character is uppercase or lowercase
 *return : 0 or 1
 */

int _isupper(int c)

{
	if (c >= 'a' && c <= 'z')

	{
		return (0);
	}

	else

	{
		return (1);
	}
}
